# pytorch-tensors-from-images
PyTorch fundamentals project focused on image tensors: loading CIFAR-10, converting images to (C, H, W) tensors, visualizing with matplotlib, and flattening/restoring tensors for downstream ML workflows.
